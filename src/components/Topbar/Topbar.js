import React from "react";
import { Grid } from "semantic-ui-react";

const TopBar = props => (
    <Grid.Row 
      as="div"
      centered
      verticalAlign={"middle"}
    >
  
    </Grid.Row>
  );

export default TopBar;